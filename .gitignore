**/*build*
**/node_modules
**/package-lock.json
**/vendor/tmp

.idea/*
!.idea/
!.idea/sonarlint.xml

.vendor

tools/native_gen/*.cache.json

# native_gen logs through base::common, whose log directory is relative to the working
# directory, so running it from the repo root leaves logs here.
/logs/